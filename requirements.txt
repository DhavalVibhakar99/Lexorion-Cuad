# Runtime dependencies for the Lexorion dashboard (local + Streamlit Cloud).
# The live inference path is: pypdf/text -> chunker -> scikit-learn baseline
# -> OpenRouter LLM escalation. No torch/transformers/spacy needed here.
# Training, notebooks, and dev tooling live in requirements-dev.txt.

streamlit>=1.28.0
pandas>=2.0.0
numpy>=1.24.0
# Pinned to the version that trained checkpoints/baseline_tfidf_logreg.joblib
# (unpickling across sklearn versions is not guaranteed to work)
scikit-learn==1.8.*
joblib>=1.3.0
pyarrow>=13.0.0
plotly>=5.15.0
pyyaml>=6.0
python-dotenv>=1.0.0
openai>=1.10.0
pypdf>=3.15.0
